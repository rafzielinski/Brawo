<article class="mb-5">
  <h1 class="mb-3"><%= content.title %></h1>

  <% if content.description.present? %>
    <div class="lead mb-4">
      <%= content.description %>
    </div>
  <% end %>

  <%= render_blocks(content.blocks) %>
</article>
