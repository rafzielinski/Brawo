# Note: This migration is not needed when using STI with brawo_cms_taxonomies table
# The migration is already created by the engine
class Create<%= table_name.camelize %> < ActiveRecord::Migration[7.1]
  def change
    # Taxonomy types use the shared brawo_cms_taxonomies table
    # No migration needed - all fields are stored in the JSON 'fields' column
  end
end

