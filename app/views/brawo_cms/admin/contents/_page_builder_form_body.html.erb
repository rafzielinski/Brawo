<%= render 'fields_card', form: form, content: content, blocks_only: true %>
<%= render 'field_tabs', form: form, content: content %>

<% unless content.persisted? %>
  <div class="brawo-form-actions">
    <%= form.submit t('brawo.actions.submit'), class: "btn btn-primary" %>
  </div>
<% end %>
