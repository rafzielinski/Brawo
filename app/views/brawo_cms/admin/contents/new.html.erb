<% if @content_type_config[:page_builder] %>
  <%= render 'page_builder_form', content: @content %>
<% else %>
  <%= render 'form', content: @content %>
<% end %>

<%= render 'slug_conflict_modal', conflict: @slug_conflict if @slug_conflict.present? %>
