<%= render 'brawo_cms/admin/shared/page_header',
           title: t('brawo.taxonomies.new_title', label: @taxonomy_type_config[:label]) %>

<div class="card">
  <div class="card-body">
    <%= render 'form', taxonomy: @taxonomy %>
  </div>
</div>
